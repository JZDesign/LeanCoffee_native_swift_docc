import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationView {
            Text("The lean coffee events will go here.")
                .navigationTitle(Text("Lean Coffee"))
        }
    }
}
